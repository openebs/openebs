sudo: false
language: go

os:
        - linux

env:
        - ARCH=x86_64

go:
        - 1.6.2

before_install:
        - sudo apt-get -qq update
        - sudo apt-get install -y lxc 

script:
        - make build
        - make install
